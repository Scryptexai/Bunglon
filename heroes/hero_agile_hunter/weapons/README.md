# Weapons

The current energy bow is a native modular model under `Visual/CharacterModel/Weapon`. A final GLB bow may replace it, provided it preserves the `ProjectileOrigin` socket position and stays visibly readable during idle and draw/release states.
