# Models

`HeroVisual` currently generates the runnable native Godot 3D model and Skeleton3D at runtime. Place the production `lyra_vesper.glb` here when available; preserve the bone/socket contract documented in `../docs/ASSET_PIPELINE.md`.
